{
    "name": "blog",
    "private": true,
    "version": "0.0.0",
    "scripts": {
      "heroku-postbuild": "cd client && npm i && npm run build",
      "start": "cd client && node server.js"
    },"engines": { 
        "node": "18.12.1" 
    }     
  }
  